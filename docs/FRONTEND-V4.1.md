FRONT-END v4.1 — pre-order mode, tier ladder, cleanup. Save this message verbatim as docs/FRONTEND-V4.1.md, commit. Create .loop-halt (wait for any cycle), branch design-v4-1 from main, work visibly; preview on push; main untouched until "publish". Front-end only. A second session is working in a separate git worktree on branch backend-v1; never touch that branch.

1. PRE-ORDER MODE COPY: hero pill becomes "Pre-order available now on the App Store"; pre-order band H2 "Pre-order now on the App Store"; remove every "opens in October" string; FAQ "When can I get it?" answer becomes "Pre-order is open now on the App Store. Release follows three to four weeks after. iPhone first, Android next." Voice rules from REDESIGN-V4 §3 apply.
2. REPLACE THE EMAIL FORM everywhere (hero, band, thanks flow) with one primary forest button "Pre-order now" that opens a modal: title "Choose your phone", two large option cards side by side — iPhone (Apple official badge, "Pre-order on the App Store") and Android (Google Play official badge, "Pre-register on Google Play") — each linking to NEXT_PUBLIC_APP_STORE_URL / NEXT_PUBLIC_PLAY_URL, "#" when unset; close on Esc/backdrop; focus trapped; accessible name; 200ms scale-in; mobile: bottom sheet. Remove the waitlist form component from the page, keep the API route untouched. /thanks redirects to /.
3. SWITCHER ARROWS: add previous/next round buttons flanking the phone (44px, white, hairline, forest icon), wired to the same state as the cards and dots; keyboard left/right when the switcher is focused; auto-advance pauses after any interaction.
4. PRICING TIER LADDER (rewrite the section around control, no ratings language): intro "The pre-order build is free and complete: onboarding, the offline solver, the week view, the three checks, whole-week regenerate, the aisle list with its total, share your week, beat this week." Protein Plan card: "Regenerate any single meal. Don't like Tuesday's dinner? Regenerate just that one and the week stays under budget and on protein." plus "Receipt reveal" and "The weekly solve challenge". Autopilot card: "Pick your replacement from a menu. Instead of the solver choosing for you, see a menu of substitutes built from what you're already buying and pick the one you want." plus "Next week planned for you every Sunday at 5pm". Keep prices and the 14-day trial line. Update the FAQ "What is free and what is paid?" to match, and the switcher card 1 copy to mention "regenerate a meal or the whole week".
5. DELIVERY COPY: everywhere the site mentions delivery pricing, it now says "what the same list costs delivered from Kroger" and is labeled estimated; remove any Instacart mention.
6. DELETE /press and /protein-index: pages, footer links, sitemap entries, press-kit link; 301 both to /. Footer Company column becomes FAQ, Support.
7. VERIFY per REDESIGN-V4 §9 (screenshots 390/1440, voice grep zero, keyboard pass incl. the modal and arrows, zero dead links, Lighthouse ≥ 90/100/100). Commit per step, push, report on a new issue "frontend v4.1". Remove .loop-halt at the end.
